#!/bin/sh
set -eu

if [ "$#" -lt 1 ]; then
    echo "Usage: kalesa-drop <game>"
    exit 1
fi

SCRIPT_DIR="$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)"
cd "$SCRIPT_DIR"
exec "$SCRIPT_DIR/kalesa" "$1"
